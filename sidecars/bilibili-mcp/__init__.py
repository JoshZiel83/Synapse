"""Bilibili MCP sidecar (LIB-WRAP over Nemo2011/bilibili-api, GPL-3.0).

This package contributes ONLY an adapter + tool surface + a thin ``serve.py``;
the entire multi-tenant Streamable-HTTP front end is owned by ``_mcp_base``
(E1: exactly one front-end implementation in the whole repo). See ``UPSTREAM.md``
for the GPL-3.0 acceptance + arms-length process-isolation note.
"""
